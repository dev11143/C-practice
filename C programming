// Program to add 2 numbers
#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("sum=%d",sum);
    return 0;

}

Modification
// same program but different function 
#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    sum=a-b;
    printf("sum=%d",sum);
    return 0;

}

// Multiplication in the same program
#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    sum=a*b;
    printf("sum=%d",sum);
    return 0;

}
// Quotient in the same program 
#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    sum=a/b;
    printf("sum=%d",sum);
    return 0;

}
// Accepting three numbers
#include<stdio.h>
int main()
{
    int a,b,c,sum;
    printf("Enter two numbers:");
    scanf("%d%d%d",&a,&b,&c);
    sum=a+b+c;
    printf("sum=%d",sum);
    return 0;

}
//Finding Average
#include<stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter two numbers:");
    scanf("%d%d",&a,&b);
    sum=(a+b)/2;
    printf("sum=%d",sum);
    return 0;

}
//new program 
#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter three Numbers:");
    scanf("%d%d%d",&a,&b,&c);
    if(a>=b&&a>=c)
    printf("%d is the largest.",a);
    else if(b>=c)
    printf("%d is the largest.",b);
    else
    printf("%d is the largest.",c);

return 0;}
    

}
 // To find smallest number

#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter three Numbers:");
    scanf("%d%d%d",&a,&b,&c);
    if(a<=b&&a<=c)
    printf("%d is the smallest.",a);
    else if(b<=c)
    printf("%d is the smallest.",b);
    else
    printf("%d is the smallest.",c);

return 0;}

// To find the large number among 4

#include<stdio.h>
int main()
{
    int a,b,c,d;
    printf("Enter three Numbers:");
    scanf("%d%d%d%d",&a,&b,&c,&d);
    if(a>=b&&a>=c&&a>=d)
    printf("%d is the largest.",a);
    else if(b>=c&&b>=d)
    printf("%d is the largest.",b);
    else
    printf("%d is the largest.",c);
    

return 0;}

// To check all are equal

#include<stdio.h>
int main()
{
    int a,b,c;
    printf("Enter any three Numbers:");
    scanf("%d%d%d",&a,&b,&c);
    if(a==b&&a==c)
    printf(" all numbers are equal");
    else 
    printf("all numbers are not equal");
    

return 0;}
//To Find the 2nd largest program
#include <stdio.h>

int secondLargest(int a, int b, int c)
{
    if ((a > b && a < c) || (a < b && a > c))
        return a;
    else if ((b > a && b < c) || (b < a && b > c))
        return b;
    else
        return c;
}

int main()
{
    int a, b, c;

    printf("Enter any three numbers: ");
    scanf("%d %d %d", &a, &b, &c);
    printf("The second largest number is: %d\n", secondLargest(a, b, c));

    return 0;
}
    
